apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "conan-server.fullname" . }}-test-connection"
  labels:
    {{- include "conan-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "conan-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
